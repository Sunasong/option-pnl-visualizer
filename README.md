# 期权策略盈亏 (P&L) 可视化工具

这是一个离线便携版的期权策略盈亏可视化工具，包含 macOS 和 Windows 两个版本。

## 下载与使用

- macOS：在 Releases 页面下载 `期权策略盈亏 (P&L) 可视化工具-mac.zip`，解压后打开 `.app`。
- Windows：在 Releases 页面下载 `Option-PnL-Visualizer-Windows.zip`，解压后双击 `一键启动.cmd` 或 `Start-Windows.bat`。

两个版本都使用本地 `vendor` 依赖，可以离线运行。
